# Where lead-form submissions are forwarded. Any endpoint that accepts a JSON POST
# works: Zapier, Make, a Slack workflow, or a CRM webhook.
# Without it, /api/contact validates the submission and logs it to the server console.
DEPLOY_LEAD_WEBHOOK=
